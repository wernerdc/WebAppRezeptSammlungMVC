@model IEnumerable<WebAppRezeptSammlungMVC.Models.Zutat>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Menge)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Einheit)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Rezept)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Lebensmittel)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Menge)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Einheit)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rezept.Bezeichnung)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Lebensmittel.Bezeichnung)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
